#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * @int: Takes in a value
 *
 * Return: 0 if successful
 */
int _abs(int value)
{
	int result;
	result = abs(value);
	return (result);
	return (0);
}
