#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * @int: Takes in a value
 *
 * Return: 0 if successful
 */
int _abs(int)
{
	int n;

	n = abs(int);
	return (n);
	return (0);
}
