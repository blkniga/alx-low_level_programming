#include "main.h"

/**
 * mul - Multiples two integers
 * @a: Takes in the first integer as an argument
 * @b: Takes the 2nd integer as an argument
 *
 * Return: The Multiplication
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
