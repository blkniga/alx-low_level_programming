#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: The string passes into the function as a char
 *
 * Return: 0
 */
int _strlen(char *s)
{
	char string;

	string = *s;
	strlen(string);

	return (0);
}
