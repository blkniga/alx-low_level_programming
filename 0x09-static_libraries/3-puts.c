#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string followed by a new line to the stdout
 * @str: The string passed as a parameter
 *
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
